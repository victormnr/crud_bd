<%= form_with(model: secretary, local: true) do |form| %>
  <% if secretary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(secretary.errors.count, "error") %> prohibited this secretary from being saved:</h2>

      <ul>
        <% secretary.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :state %>
    <%= form.text_field :state %>
  </div>

  <div class="field">
    <%= form.label :coordinator %>
    <%= form.text_field :coordinator %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
